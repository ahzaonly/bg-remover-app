version: '3'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 8000:8000
    volumes:
      - ./uploads:/app/uploads
    environment:
      - PORT=8000
      - PYTHONUNBUFFERED=1
